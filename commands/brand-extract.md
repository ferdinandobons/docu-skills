# /brand-extract

Extract a Brand Profile from an Office template.

Run from the plugin root (or set `DOCU_SKILLS_ROOT` to it):

```bash
python scripts/brandkit/cli.py extract --name <brand> --template <template.docx|template.pptx|template.xlsx> --scope project
```
